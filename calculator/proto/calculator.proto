syntax = "proto3";

package calculator;

import "sum.proto";
import "prime.proto";

option go_package = "github.com/urko-b/grpc-go-urko/calculator/proto";

service CalculatorService {
  rpc Sum(SumRequest) returns (SumResponse);
  rpc Prime(PrimeRequest) returns (stream PrimeResponse);
}